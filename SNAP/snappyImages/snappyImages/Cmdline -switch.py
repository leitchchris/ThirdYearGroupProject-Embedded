
In Portal's command line window to setup a Synapse USB device to show up as a Virtual COM Port (VCP) 
you would use the command "root.setup_usb_as_vcp()" 
to change the device back you would use "root.setupSynapseUsb()".
